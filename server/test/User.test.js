/* eslint-disable no-undef */

describe('Test User Controller Api call', () => {
  it('Get one User', async () => {
  });

  it('Get one User (fail)', async () => {
  });

  it('Create a new user', async () => {
  });
});
